<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.protopo.previewplace">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/icono"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".registro_usuarios"
            android:configChanges="screenSize|orientation"
            android:label="PREVIE WPLACE">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Ajustes"
            android:label="@string/lbl_ajustes_name"
            android:configChanges="screenSize|orientation" />
        <activity
            android:name=".niv_dif_cont"
            android:configChanges="screenSize|orientation"
            android:label="@string/lblDiferencial" />
        <activity
            android:name=".activity_niv_perfil"
            android:configChanges="screenSize|orientation"
            android:label="@string/lblPerfil" />
        <activity
            android:name=".test_graphis"
            android:configChanges="screenSize|orientation" />

        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|orientation"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".Ayuda_comentarios"
          android:label="@string/lbl_comentarios_name"/>

        <activity android:name=".acerca"
         android:label="@string/lbl_acerca_name" />

    </application>

</manifest>